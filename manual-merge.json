{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Every update waits for a person. For repos where nothing gates a merge (no CI) or where a merge is itself a deploy. Extend it after the main preset: `github>mac-lucky/actions-shared-workflows:manual-merge`",
  "packageRules": [
    {
      "matchPackageNames": ["*"],
      "automerge": false
    }
  ]
}
