{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices", ":semanticCommits"],
  "dependencyDashboard": false,
  "postUpdateOptions": ["gomodTidy"],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 0,
  "packageRules": [
    {
      "description": "Automerge safe update types, pre-1.0 minor bumps included. GitHub does the merge itself the moment the required check goes green, so this no longer waits for the next daily Renovate run",
      "matchUpdateTypes": ["minor", "patch", "pin", "pinDigest", "digest", "lockFileMaintenance"],
      "automerge": true
    },
    {
      "description": "Majors stay manual and get flagged; the Dependency Dashboard issue lists them per repo",
      "matchUpdateTypes": ["major"],
      "labels": ["major"]
    },
    {
      "description": "Callers ride the shared workflows at @master by design; never pin or update those refs",
      "matchDepNames": ["mac-lucky/actions-shared-workflows"],
      "pinDigests": false,
      "enabled": false
    },
    {
      "description": "svelte-check cannot typecheck under TypeScript 7 yet; lift once the Svelte toolchain supports it",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7.0.0"
    }
  ]
}
